version: '3'

services:
    nginxhello:
        image: nginxdemos/hello:latest
        # image: hello-world
        ports:
            - '8686:80'
        volumes:
            - ../..:/workspaces:cached
